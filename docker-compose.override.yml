services:
  claude-code-router:
    build: .
    ports:
      - "3456:3456"
    environment:
      - OPENAI_API_KEY=value
    volumes:
      - ./config.json:/root/.claude-code-router/config.json
    restart: unless-stopped
